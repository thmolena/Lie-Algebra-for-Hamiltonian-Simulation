# Exact Python environment used to generate every result, table, and figure in
# this submission.  Tested with CPython 3.13.12.
#
# Pinned for reproduction of the meaningful results to full double precision.
# Values that sit at the floating-point floor (oracle residuals ~1e-13 and the
# weight-5 projected row) depend on the BLAS/LAPACK build and the order of
# floating-point accumulation, so their last digits can differ on another
# machine; this is expected and is discussed in the paper's finite-precision
# appendix.  The physically meaningful numbers -- all baselines, the projected
# w<=3 / w<=4 results, the learned-transfer errors, and R^2 -- are stable.
#
# Install with:  python -m pip install -r requirements.txt

numpy==2.4.3
scipy==1.17.1
pandas==3.0.2
matplotlib==3.10.8

# Required only for the learned-residual experiment (learned_residual.py).
# The dense-matrix experiments and all other figures build without it.
torch==2.11.0
