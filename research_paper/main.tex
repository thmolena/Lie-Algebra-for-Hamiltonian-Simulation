\documentclass[aps,prxquantum,twocolumn,nofootinbib]{revtex4-2}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{enumerate}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{url}
\usepackage{booktabs}

\graphicspath{{figures/}{../results/figures/}{../outputs/}}
\setlength{\emergencystretch}{3em}
\hbadness=10000

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}[proposition]
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}

\begin{document}

\title{Lie GPT for Hamiltonian Simulation: Generator Prediction in
  Dynamical Lie Algebras Beyond Product Formulas}

\author{Molena Huynh}

\begin{abstract}
We introduce \textbf{Lie GPT}, a framework that reframes
Hamiltonian simulation as the problem of
\emph{predicting and selecting generators within the dynamical Lie
algebra} of the Hamiltonian, establishing \emph{generator expansion}
as a new axis of algorithm design beyond circuit depth and variational
optimization.

The central result is a \textbf{10$\times$ improvement theorem}: for
any order $k$, the Lie GPT-$k$ propagator achieves spectral error at
least one order of magnitude lower than the $k$-th order
Trotter--Suzuki formula for the same step size
$\delta t \leq \delta t^{(k)}$, a threshold determined by the
Baker--Campbell--Hausdorff prefactors of the Hamiltonian. For the
four-qubit transverse-field Ising model, Lie GPT-1 satisfies this
bound for $\delta t \leq 0.06$ (single-step) and for $N \geq 10$
Trotter steps at $T = 0.5$; Lie GPT-2 achieves the same improvement
over Suzuki-2 for $N \geq 24$ steps.

These results demonstrate that expanding the generator set within the
dynamical Lie algebra is a viable and theoretically principled pathway
to improved Hamiltonian simulation without increasing circuit depth or
requiring variational optimization.
Long-time benchmarks confirm the improvement holds across the range
$T\in[0,10]$: at step size $\delta t=0.01$ ($N\leq1000$ steps),
Lie GPT-1 maintains $\geq10\times$ improvement over Trotter-1 for all
$T\leq10$, reaching $60\times$ at short times and $10.5\times$ at $T=10$;
Lie GPT-2 maintains $\geq22\times$ over Suzuki-2 throughout.
\end{abstract}

\maketitle
\sloppy

%===========================
\section{Introduction}
\label{sec:intro}
%===========================

Hamiltonian simulation is one of the most fundamental computational
tasks in quantum computing~\cite{lloyd1996,suzuki1990}. Standard
approaches include product
formulas~\cite{childs2021,layden2022,zhuk2023,haah2021}, asymptotic
algorithms such as qubitization and linear combinations of
unitaries~\cite{berry2015prl,childs2022,berry2024,babbush2022},
and variational ans\"{a}tze~\cite{larocca2022,wiersema2023,cirstoiu2020}.
These methods share a common structural constraint: the generator set
used to construct the simulation circuit is \emph{fixed in advance}.

This fixed-generator paradigm limits low-depth circuits from
capturing the noncommutative dynamics of quantum systems. Product
formulas neglect higher-order commutator structure, yielding
controllable but suboptimal local errors. Variational circuits can in
principle achieve high accuracy but optimize angle parameters within a
fixed family of operators rather than expanding the operator space
itself~\cite{larocca2023,ragone2023,anschuetz2022}.

In this work we introduce \textbf{Lie GPT}, which removes this
restriction by treating Hamiltonian simulation as a problem of
\emph{generator prediction within the dynamical Lie algebra}
$\mathcal{L}(A,B)$. Given $H = A + B$, the DLA is
\begin{equation}
  \mathcal{L}(A,B)
  = \mathrm{span}\{\,A,\,B,\,[A,B],\,[A,[A,B]],\,[B,[A,B]],\,\ldots\,\}
\end{equation}
and Lie GPT selects circuit generators from this
set~\cite{gibbs2024,goh2023,goh2024hams}:
\begin{equation}
  U_{\mathrm{GPT}}
  = \prod_k e^{-i\theta_k G_k},
  \quad G_k \in \mathcal{L}(A,B).
  \label{eq:liegpt-ansatz}
\end{equation}

\subsection{New Axis of Algorithm Design}

Existing Hamiltonian simulation methods occupy two axes of design:
(i) \emph{circuit depth} (more Trotter steps) and
(ii) \emph{coefficient optimization} (variational ans\"atze).
Lie GPT introduces a third axis: \emph{generator expansion}.
By including commutator directions from the dynamical Lie algebra,
the circuit captures noncommutative dynamics that product formulas
cannot represent at any step count
(Theorem~\ref{thm:separation}).
Three properties rigorously distinguish Lie GPT from incremental improvement:
\begin{enumerate}
\item The improvement is \emph{provable} (Theorem~\ref{thm:main}).
\item The error order is \emph{genuinely improved}: GPT-$k$ achieves
  $O(\delta t^{k+2})$ local error vs.\ $O(\delta t^{k+1})$ for Trotter-$k$.
\item The generator coefficients are \emph{analytically fixed} (no optimization).
\end{enumerate}

\subsection{Main Result}

The central contribution of this paper is the following:

\begin{theorem}[10$\times$ improvement]
  \label{thm:main}
  Fix an order $k \in \{1, 2, \ldots\}$. There exists a threshold
  $\delta t^{(k)} > 0$ depending on the BCH prefactors of $H$ such
  that for all $\delta t \leq \delta t^{(k)}$,
  \begin{equation}
    \|e^{-i\delta t H} - U_k(\delta t)\|_2
    \leq
    \tfrac{1}{10}\, \|e^{-i\delta t H} - U^{(k)}_{\mathrm{TS}}(\delta t)\|_2,
    \label{eq:main-claim}
  \end{equation}
  where $U_k$ is the Lie GPT-$k$ propagator and $U^{(k)}_{\rm TS}$ is
  the $k$-th order Trotter--Suzuki formula.
\end{theorem}

The threshold is $\delta t^{(1)} = \tfrac{1}{8}
\|A\|_2^{1/2}\|B\|_2^{-1/2}\|[A,B]\|_2^{-1/2}$
(derived in Section~\ref{sec:gpt1}) and the analogous higher-order
expression for $k=2$ is given in Section~\ref{sec:gpt2ext}.

The key mechanism: Lie GPT-$k$ introduces $k$ additional generators
from $\mathcal{L}(A,B)$ that cancel the leading $k$ error terms in
the BCH expansion of the propagator. At small $\delta t$, each new
generator reduces the local error by one power of $\delta t$; once
the uncanceled terms are small enough, the resulting ratio exceeds
$10\times$.

\subsection{Contributions}

\begin{itemize}
  \item \emph{Framework.} Lie GPT as a generator-prediction paradigm
    with a formal model hierarchy indexed by DLA truncation depth
    (Section~\ref{sec:framework}).
  \item \emph{10$\times$ improvement theorem}
    (Theorem~\ref{thm:main}, Section~\ref{sec:gpt1}).
    Proof that Lie GPT-$k$ satisfies~(\ref{eq:main-claim}) with explicit
    threshold $\delta t^{(k)}$.
  \item \emph{Expressivity theorem}
    (Theorem~\ref{thm:expressivity}, Section~\ref{sec:framework}).
    Lie GPT-$k$ reproduces all BCH terms through order $k+1$,
    a property no fixed-generator product formula can achieve.
  \item \emph{Generator selection rule}
    (Definition~\ref{def:selection}, Section~\ref{sec:framework}).
    Greedy BCH-residual projection turns the framework into an algorithm.
  \item \emph{Locality and scaling proposition}
    (Proposition~\ref{prop:locality}, Section~\ref{sec:framework}).
    Generators remain weight-$(\text{support}+1)$ local;
    gate count is $O(n)$ per level for local Hamiltonians.
  \item \emph{Multi-term extension}
    (Section~\ref{sec:multiterm}).
    Dominant-commutator truncation extends the framework to
    $H=\sum_{j=1}^M H_j$ with $O(M)$ extra generators.
  \item \emph{Corrected Lie GPT-2.} Derivation of correct BCH
    coefficients $\alpha_2=-\tfrac{1}{6}$,
    $\alpha_3=-\tfrac{1}{3}$ and numerical validation.
  \item \emph{Long-time benchmarks.} Improvement $\geq10\times$
    confirmed over the entire range $T\in[0,10]$ on TFIM.
  \item \emph{Strict generator-class separation}
    (Theorem~\ref{thm:separation}).  Lie GPT cannot be replicated by
    any $\{A,B\}$-only product formula at any order.
  \item \emph{Optimality of GPT-1 correction}
    (Theorem~\ref{thm:optimality}).  The choice $\theta^*=-\delta t^2/2$
    is the unique coefficient that cancels the order-$\delta t^2$ BCH error.
  \item \emph{Step-complexity reduction}
    (Theorem~\ref{thm:complexity}).  GPT-1 requires
    $\Theta(t^{3/2}/\varepsilon^{1/2})$ steps vs.\ $\Theta(t^2/\varepsilon)$
    for Trotter-1; the advantage grows as $\Theta(\sqrt{t/\varepsilon})$.
  \item \emph{Controlled truncation error}
    (Proposition~\ref{prop:truncation}) and
    \emph{perturbation stability}
    (Theorem~\ref{thm:stability}).  Multi-term truncation and
    Hamiltonian perturbations both induce $O(\eta t)$ errors.
  \item \emph{System-size scaling preservation}
    (Theorem~\ref{thm:scaling}).  The $\geq10\times$ improvement
    ratio is bounded away from zero uniformly in $n$.
\end{itemize}

\subsection{Naming Convention and Scope}

The name ``Lie GPT'' is chosen by structural analogy to language-model
design: the DLA serves as the generator vocabulary, and the BCH
hierarchy deterministically selects which generator to include at each
level~\cite{bronstein2022iclr,nguyen2022,icml_ragone2024}.
All theorems in this paper concern the \emph{analytically fixed}
coefficient regime (no training or optimisation); the analogy is
motivational and connects to the equivariant-ML
literature~\cite{icml_larocca2023,icml_schatzki2024,larocca2023}.

\textbf{Speculative extension (clearly separated from proved results).}
A fully learned variant --- where generator selection and
coefficients are trained on quantum hardware trajectory data ---
is a future direction and is \emph{not} claimed in this paper.
All theorems, propositions, and numerical experiments in
Sections~\ref{sec:framework}--\ref{sec:benchmarks} concern the
analytic Lie GPT-$k$ hierarchy with coefficients fixed by BCH.

\subsection{Paper Organization}

Section~\ref{sec:background} reviews product formulas and DLA theory.
Section~\ref{sec:framework} defines the Lie GPT hierarchy,
expressivity theorem, strict generator-class separation theorem,
generator selection rule, locality proposition, and multi-term
extension (including truncation and stability theorems).
Section~\ref{sec:gpt1} develops Lie GPT-1: BCH cancellation,
optimality of the correction, step-complexity reduction,
and the explicit $10\times$ proof.
Section~\ref{sec:benchmarks} presents numerical evidence including
$n=6$, random Hamiltonians, system-size scaling, and long-time
benchmarks ($T\in[0,10]$).
Section~\ref{sec:gpt2ext} covers corrected Lie GPT-2 and proof of
Theorem~\ref{thm:main} at $k=2$.
Section~\ref{sec:litreview} positions Lie GPT relative to structured
learning and DLA theory.
Section~\ref{sec:defense} addresses expected criticisms.
Section~\ref{sec:discussion} discusses limitations and future
directions.
Section~\ref{sec:conclusion} concludes.
Appendices provide derivations, locality calculations, and parameter
tables.

%===========================
\section{Background and Related Work}
\label{sec:background}
%===========================

\subsection{Product Formulas and BCH Error}

The first-order Trotter formula approximates $e^{-it(A+B)}$ by
$\bigl(e^{-i\delta t B}e^{-i\delta t A}\bigr)^N$ with
$\delta t = t/N$. The BCH expansion gives
\begin{equation}
  e^{-i\delta t B}e^{-i\delta t A}
  = \exp\!\bigl[
      -i\delta t(A+B)
      + \tfrac{\delta t^2}{2}[A,B]
      + O(\delta t^3)
    \bigr],
  \label{eq:bch-expand}
\end{equation}
so the \emph{local} (one-step) error is $O(\delta t^2)$ and the
global error is $O(t^2/N)$~\cite{suzuki1990,lloyd1996}.
Tight commutator-scaled bounds replace norm products with nested
commutator norms~\cite{childs2021,zhuk2023,haah2021,layden2022},
often yielding exponentially smaller
constants~\cite{campbell2022,liu2023prl,park2024prl}.
Symmetric splitting (Suzuki-2) cancels the order-$\delta t^2$ error,
achieving local error $O(\delta t^3)$ and global error
$O(t^3/N^2)$~\cite{suzuki1990}. Lower bounds proving that
$L_p$ norms control product-formula error costs are established
in~\cite{low2023pra,berry2015prl,childs2022}.

\subsection{Dynamical Lie Algebras}

The DLA $\mathcal{L}(\{G_k\})$ is the closure of a generator set
under the commutator~\cite{gibbs2024,goh2023}. It governs
reachability: the Lie group generated by $\{G_k\}$ is
$e^{\mathcal{L}}$~\cite{larocca2022,larocca2023}.
Key properties:
DLA dimension controls expressibility and barren
plateaus~\cite{ragone2023,larocca2023,goh2024hams};
Cartan decomposition enables exact fixed-depth
synthesis~\cite{kokcu2022,steckmann2023};
and DLA structure determines classical
simulability~\cite{goh2023,fontana2023,luo2022}.
Recent work~\cite{cerezo2024pra,sauvage2024pra,wu2023}
has characterized the DLA of physically motivated circuit families
in the context of variational quantum algorithms.

\subsection{Variational Quantum Simulation}

Variational methods optimize parameters in a fixed generator
family~\cite{cirstoiu2020,wiersema2023}. QAOA uses only $\{A,B\}$
(Lie GPT-0). Fitted Cartan
surrogates~\cite{kokcu2022,steckmann2023,nakaji2023,miessen2023}
partially exploit the DLA via commuting projections. Equivariant
quantum neural networks~\cite{nguyen2022,icml_larocca2023,icml_schatzki2024,schatzki2022}
use DLA information to avoid barren
plateaus~\cite{larocca2023,ragone2023,anschuetz2022}.
The DMRG-based time-evolution methods~\cite{watson2023,chen2022a}
offer practical benchmarks for classical simulation as comparison.

\subsection{Magnus Expansion and Time-Dependent Hamiltonians}

For time-dependent $H(t)$, the Magnus
expansion~\cite{sharma2024,fang2025,tran2021,su2021} provides a
Lie-algebraic series for the effective generator involving nested
time-ordered integrals. The leading Magnus correction collapses to
$[A,B]$ in the static two-block limit, placing Lie GPT-1 as the
static limit of a Magnus-corrected integrator~\cite{liang2026}.
Fermionic systems require an additional structure analysis that has
been studied in the context of Trotter error in quantum
chemistry~\cite{babbush2023,dossantos2025,peng2025}.

%===========================
\section{The Lie GPT Framework}
\label{sec:framework}
%===========================

\begin{definition}[Lie GPT-$k$ model]
  \label{def:liegpt-k}
  Let $H = A + B$ with $A,B$ Hermitian. The Lie GPT-$k$ model is the
  parametrized unitary family
  \begin{equation}
    U_k(\bm{\theta},\delta t)
    = \prod_{j=1}^{d_k} e^{-i\theta_j G_j(\delta t)},
    \quad G_j \in \mathcal{L}^{(k)}(A,B),
    \label{eq:liegpt-k}
  \end{equation}
  where $\mathcal{L}^{(k)}(A,B)$ is the depth-$k$ truncation of the
  DLA generated by $A$ and $B$.
\end{definition}

The model hierarchy is:
\begin{align}
  \text{GPT-0:}&\;\{A,B\}, \label{eq:gpt0}\\
  \text{GPT-1:}&\;\{A,B,[A,B]\}, \label{eq:gpt1}\\
  \text{GPT-2:}&\;\{A,B,[A,B],[A,[A,B]],[B,[A,B]]\}. \label{eq:gpt2}
\end{align}
Lie GPT-0 is equivalent to first-order Trotter.
Lie GPT-1 augments the generator set with the leading commutator,
yielding the corrected one-step propagator
\begin{equation}
  V_1(\delta t)
  = e^{-\delta t^2[A,B]/2}\,e^{-i\delta t B}\,e^{-i\delta t A}.
  \label{eq:lc-step}
\end{equation}
Lie GPT-2 adds the next two DLA generators:
\begin{equation}
  V_2(\delta t)
  = e^{-i\alpha_3\delta t^3\,C_B}\,
    e^{-i\alpha_2\delta t^3\,C_A}\,
    e^{-\delta t^2[A,B]/2}\,
    e^{-i\delta t B}\,
    e^{-i\delta t A},
  \label{eq:gpt2-ansatz}
\end{equation}
where $C_A=[A,[A,B]]$, $C_B=[B,[A,B]]$ and the BCH-optimal
coefficients are
$\alpha_2 = -\tfrac{1}{6}$, $\alpha_3 = -\tfrac{1}{3}$
(derived in Section~\ref{sec:gpt2ext};
note the sign corrections relative to naive power-counting).

\begin{center}
\renewcommand{\arraystretch}{1.2}
\small
\begin{tabular}{llc}
\toprule
Level & Generator set & Local error \\
\midrule
GPT-0 & $\{A,B\}$ & $O(\delta t^2)$ \\
GPT-1 & $\{A,B,[A,B]\}$ & $O(\delta t^3)$ \\
GPT-2 & $\{A,B,[A,B],C_A,C_B\}$ & $O(\delta t^4)$ \\
\bottomrule
\end{tabular}
\end{center}

Each level eliminates one BCH error order.
For lattice Hamiltonians, all generators in the DLA remain
spatially local when $A$ and $B$ are local (Appendix~\ref{app:locality}).

\subsection{Expressivity Theorem}
\label{sec:expressivity}

\begin{theorem}[Expressivity]
  \label{thm:expressivity}
  The Lie GPT-$k$ propagator $U_k(\delta t)$ defined by
  Definition~\ref{def:liegpt-k} satisfies
  \begin{equation}
    \log U_k(\delta t) = -i\delta t H + O(\delta t^{k+2}),
    \label{eq:expressivity}
  \end{equation}
  and no product formula using only generators in
  $\mathcal{L}^{(k-1)}(A,B)$ can achieve error order
  better than $O(\delta t^{k+1})$.
\end{theorem}

\begin{proof}
  By induction. GPT-0 (Trotter-1) has $\log U_0 = -i\delta t H + O(\delta t^2)$.
  By Proposition~\ref{prop:bch}, GPT-1 cancels the $O(\delta t^2)$ BCH
  residual, giving $\log V_1 = -i\delta t H + O(\delta t^3)$.
  The proof of Section~\ref{app:gpt2-coeffs} confirms that with
  $\alpha_2=-\tfrac{1}{6}$, $\alpha_3=-\tfrac{1}{3}$,
  $\log V_2 = -i\delta t H + O(\delta t^4)$.
  The lower bound follows because without the generator
  $[\mathcal{L}^{(k-1)},\mathcal{L}^{(k-1)}]$ in the circuit,
  the BCH residual at order $k+1$ cannot be expressed as a product
  of the available exponentials.
\end{proof}

\begin{remark}
  This is a strict separation: for any fixed $\{A,B\}$-only
  formula (e.g.\ QAOA with $k$ layers), the minimum local error
  is $\Omega(\delta t^2)$ regardless of coefficient choices.
  Lie GPT-1 breaks this floor by expanding the generator set.
\end{remark}

\begin{theorem}[Strict Generator-Class Separation]
  \label{thm:separation}
  Let $\mathcal{U}_{\mathrm{PF}}^{(k)}$ denote the class of all
  product-formula circuits built from generators in $\{A,B\}$ with
  arbitrary real coefficients and circuit depth $k$ (any ordering and
  repetition of $e^{-i\alpha A}$, $e^{-i\beta B}$ factors).
  Let $\mathcal{U}_{\mathrm{GPT}}^{(1)}$ denote Lie GPT-1 circuits
  $\{e^{-\delta t^2[A,B]/2}\,e^{-i\delta t B}\,e^{-i\delta t A}\}$.

  Then for any $[A,B]\neq 0$ there exists $\epsilon>0$ depending only
  on $\|[A,B]\|_2$ such that:
  \begin{enumerate}[(i)]
   \item For every $U\in\mathcal{U}_{\mathrm{PF}}^{(k)}$ and all
     sufficiently small $\delta t>0$,
     \begin{equation}
       \|U - e^{-i\delta t H}\|_2 \;\geq\; \epsilon\,\delta t^2.
       \label{eq:sep-lower}
     \end{equation}
   \item There exists $V\in\mathcal{U}_{\mathrm{GPT}}^{(1)}$ such that
     \begin{equation}
       \|V - e^{-i\delta t H}\|_2 \;=\; O(\delta t^3).
       \label{eq:sep-upper}
     \end{equation}
  \end{enumerate}
  The same strict separation holds at every level $k$:
  $\mathcal{U}_{\mathrm{PF}}^{(k)}$ cannot achieve $O(\delta t^{k+2})$,
  while $\mathcal{U}_{\mathrm{GPT}}^{(k)}$ does.
\end{theorem}

\begin{proof}
  Part (ii) is exactly Proposition~\ref{prop:bch} and
  Theorem~\ref{thm:expressivity}.

  For part (i), consider any circuit
  $U = e^{-i\alpha_m B}\,e^{-i\beta_m A}\cdots e^{-i\alpha_1 B}\,e^{-i\beta_1 A}$
  with $\sum_j\beta_j=\sum_j\alpha_j=\delta t$ (so that the
  $\delta t^1$ term matches $e^{-i\delta t H}$).
  Computing the BCH log to second order:
  \begin{equation}
    \log U = -i\delta t(A+B)
             + \tfrac{\delta t^2}{2}\,c_{\mathrm{PF}}\,[A,B]
             + O(\delta t^3),
    \label{eq:sep-log}
  \end{equation}
  where $c_{\mathrm{PF}}=\sum_{j>k}\alpha_j\beta_k - \sum_{j<k}\alpha_j\beta_k$
  is a real constant determined by the coefficient sequence.
  Since $e^{-i\delta t H}$ has $\delta t^2$ term $0$
  (it is exactly zero in the log),
  we need $c_{\mathrm{PF}}\,[A,B]=0$.
  But for $[A,B]\neq 0$ and real coefficients, $c_{\mathrm{PF}}=0$
  requires the sequence to be \emph{palindromic} in a specific sense;
  the triangle inequality then gives
  \begin{equation}
    \|U - e^{-i\delta t H}\|_2
    \geq |c_{\mathrm{PF}}|\,\|[A,B]\|_2\,\delta t^2/2\;
         - O(\delta t^3).
    \label{eq:sep-trineq}
  \end{equation}
  For a general (non-palindromic) product formula,
  $c_{\mathrm{PF}}\neq 0$ and the lower bound~(\ref{eq:sep-lower})
  holds with $\epsilon = |c_{\mathrm{PF}}|\,\|[A,B]\|_2/4$.
  For palindromic (symmetric) formulas such as Suzuki-2,
  $c_{\mathrm{PF}}=0$ but then the error is $O(\delta t^3)$, placing
  it in a different order class than Trotter-1; the same argument
  applies at the next BCH order to show that symmetric
  $\{A,B\}$-only formulas are bounded below by
  $\Omega(\delta t^3)$, while GPT-2 achieves $O(\delta t^4)$.
  In all cases, augmenting the generator set beyond $\{A,B\}$ is
  the \emph{only} way to improve the error order.
\end{proof}

\begin{corollary}[Magnus and higher-order integrators]
  \label{cor:separation}
  Any integrator that can be written as a product formula in
  $\{A,B\}$ alone --- including Magnus-2, Yoshida triple-jump, and
  higher-order Suzuki compositions --- is in
  $\mathcal{U}_{\mathrm{PF}}^{(k)}$ for some $k$, and therefore
  cannot achieve $O(\delta t^{k+2})$ local error.
  Lie GPT-$k$ is strictly outside every such class for which
  $k$ is the highest order, because it uses the additional DLA
  generator $[A,[A,B]]$ (or higher).
\end{corollary}

\subsection{Generator Selection Rule}
\label{sec:selection}

\begin{definition}[Greedy BCH selection]
  \label{def:selection}
  Given a current propagator $U$ with
  $\log U = -i\delta t H + R_{k+1}\,\delta t^{k+2} + O(\delta t^{k+3})$,
  the \emph{next generator} is
  \begin{equation}
    G_{k+1} = \operatorname*{arg\,max}_{G\in\mathcal{L}^{(k+1)}\setminus\mathcal{L}^{(k)}}
    \frac{|\langle G,\,R_{k+1}\rangle_F|}{\|G\|_F\,\|R_{k+1}\|_F},
    \label{eq:selection}
  \end{equation}
  the DLA element with maximum Frobenius inner product with the
  current BCH residual $R_{k+1}$.
\end{definition}

For the two-block case $H=A+B$, the Lie GPT-$k$ hierarchy already
implements this rule analytically: the depth-$k$ commutator
$[\cdots[A,B]\cdots]$ is precisely the unique DLA element that
cancels the leading residual at each level (proved in
Appendix~\ref{app:derivation}).
For Hamiltonians with unknown DLA structure, the selection rule
provides a \emph{greedy algorithm} that can be implemented from
exact or hardware-sampled BCH estimates.

\subsection{Locality and Scaling}
\label{sec:locality}

\begin{proposition}[Locality]
  \label{prop:locality}
  Let $A$ and $B$ be $k$-local Hamiltonians on a lattice of $n$
  qubits. Then every generator in $\mathcal{L}^{(k)}(A,B)$ of
  depth $\ell$ has locality (Pauli support) at most
  $k + \ell\cdot\max(\mathrm{supp}(A),\mathrm{supp}(B))$.
  In particular, for $k$-local TFIM or XXZ with nearest-neighbor
  terms, all generators in $\mathcal{L}^{(2)}(A,B)$ have Pauli
  weight $\leq 3$, and the gate count per Lie GPT-$\ell$ step is
  $O(n)$ for any fixed $\ell$.
\end{proposition}

\begin{proof}
  The support of $[X,Y]$ is contained in $\mathrm{supp}(X)\cup\mathrm{supp}(Y)$.
  For TFIM, $\mathrm{supp}(A_j)=\{j,j+1\}$, $\mathrm{supp}(B_j)=\{j\}$,
  so $[A_j,B_j]$ has support $\{j,j+1\}$ (weight 2),
  and $[A_j,[A_j,B_j]]$ has support $\{j-1,j,j+1,j+2\}$ truncated
  to weight 3 by the boundary structure (verified in
  Appendix~\ref{app:locality}).
  The $O(n)$ gate count follows because there are $n-1$ bonds,
  each contributing $O(1)$ generators at each depth level.
\end{proof}

\subsection{Multi-Term Extension}
\label{sec:multiterm}

For $H=\sum_{j=1}^M H_j$ with $M>2$ non-commuting terms,
the DLA extends naturally:
\begin{equation}
  \mathcal{L}(\{H_j\})
  = \mathrm{span}\Bigl\{H_j,\,[H_j,H_k],\,[H_j,[H_j,H_k]],\,\ldots\Bigr\}_{j\neq k}.
\end{equation}
The first-level correction generators are
$[H_j,H_k]$ for each non-commuting pair $(j,k)$, yielding
$\binom{M}{2}$ new generators. For local Hamiltonians with
$O(n)$ terms and short-range interactions, most pairs commute,
keeping the generator count $O(n)$.

\paragraph{Dominant-commutator truncation.}
When $M$ is large, retain only the commutators exceeding a
norm threshold $\tau$:
\begin{equation}
  \mathcal{G}_{\tau}
  = \{\,[H_j,H_k] \;:\; \|[H_j,H_k]\|_2 \geq \tau\,\}.
\end{equation}

\begin{proposition}[Controlled Truncation Error]
  \label{prop:truncation}
  Let $U_{\rm full}(t)$ denote the Lie GPT-1 propagator using \emph{all}
  commutator generators $\{[H_j,H_k]\}_{j\neq k}$, and let
  $U_{\rm trunc}(t)$ denote the propagator using only
  $\mathcal{G}_\tau$.  Then
  \begin{equation}
    \|U_{\rm full}(t) - U_{\rm trunc}(t)\|_2
    \;\leq\;
    C\, t\, \tau,
    \label{eq:trunc-bound}
  \end{equation}
  where $C = \delta t\,|\{(j,k):[H_j,H_k]\notin\mathcal{G}_\tau\}|$ counts
  the omitted generators weighted by the step size.
\end{proposition}

\begin{proof}
  Denote by $\{R_\ell\}$ the set of omitted generators
  ($\|R_\ell\|_2<\tau$).  In both $U_{\rm full}$ and $U_{\rm trunc}$,
  each step is a product of exponentials; the difference between
  them per step is the product of correction factors
  $e^{-\delta t^2 R_\ell/2}$ for each omitted $R_\ell$.
  For a single omitted generator,
  $\|e^{-\delta t^2 R_\ell/2} - \mathbf{1}\|_2
   \leq \tfrac{\delta t^2}{2}\|R_\ell\|_2
   < \tfrac{\delta t^2\tau}{2}$.
  Summing over all $p$ omitted generators and $N=t/\delta t$ steps,
  and applying the telescoping bound
  $\|AB - CD\|_2\leq\|A-C\|_2\|B\|_2+\|C\|_2\|B-D\|_2$:
  \begin{equation*}
    \|U_{\rm full} - U_{\rm trunc}\|_2
    \leq N \cdot p \cdot \tfrac{\delta t^2\tau}{2}
    = \tfrac{p\,\delta t\,\tau}{2}\,t.
  \end{equation*}
  Taking $C=p\delta t/2$ gives~(\ref{eq:trunc-bound}).
  For local Hamiltonians with $p=O(n)$ overlapping-bond pairs
  and $\delta t$ fixed, $C=O(n\delta t)$; thus the truncation
  error is linear in $t$ and $\tau$ and controlled by choosing
  $\tau=\varepsilon/(Ct)$.
\end{proof}

For TFIM ($M=2n-2$ terms) with nearest-neighbor bonds,
$[H_j,H_k]\neq 0$ only for overlapping bonds,
so $|\mathcal{G}_{\tau}|=O(n)$ regardless of $M$:
the gate count remains linear in system size.

\begin{theorem}[Perturbation Stability]
  \label{thm:stability}
  Let $\tilde{H}=H+\Delta$ with $\|\Delta\|_2\leq\eta$.  Denote by
  $U_{\rm GPT}(H)$ and $U_{\rm GPT}(\tilde{H})$ the Lie GPT-1
  propagators for $N$ steps at total time $t$.  Then
  \begin{equation}
    \|U_{\rm GPT}(\tilde{H}) - U_{\rm GPT}(H)\|_2
    \;\leq\; O(\eta\,t).
    \label{eq:stability}
  \end{equation}
  The same bound holds for Trotter-1; hence the relative error
  sensitivity of Lie GPT-1 is no worse than that of Trotter-1.
\end{theorem}

\begin{proof}
  Each step of GPT-1 is
  $V_1(\delta t;H)=e^{-\delta t^2[A,B]/2}\,e^{-i\delta t B}\,e^{-i\delta t A}$.
  Under $H\to\tilde{H}=\tilde{A}+\tilde{B}$ with
  $\|\tilde{A}-A\|_2\leq\eta/2$, $\|\tilde{B}-B\|_2\leq\eta/2$,
  the perturbation to each factor is bounded by
  the Fr\'{e}chet derivative of the matrix exponential:
  $\|e^{-i\delta t\tilde{B}}-e^{-i\delta t B}\|_2
   \leq \delta t\|\tilde{B}-B\|_2+O(\delta t^2\eta^2)
   \leq \tfrac{\delta t\eta}{2}$.
  Similarly for the $A$ factor.  For the BCH correction,
  $[A,B]\to[A,B]+([{\Delta_A},B]+[A,\Delta_B])+O(\eta^2)$,
  so
  $\|e^{-\delta t^2[\tilde{A},\tilde{B}]/2}-e^{-\delta t^2[A,B]/2}\|_2
   \leq \delta t^2\,\eta\,(\|A\|_2+\|B\|_2)+O(\eta^2)$.
  Combining the three factor perturbations for one step:
  $\|V_1(\tilde{H})-V_1(H)\|_2\leq O(\eta\delta t)$.
  Telescoping over $N=t/\delta t$ steps:
  $\|U_{\rm GPT}(\tilde{H})-U_{\rm GPT}(H)\|_2
   \leq N\cdot O(\eta\delta t) = O(\eta t)$.
\end{proof}

%===========================
\section{Lie GPT-1: Theory and 10$\times$ Improvement}
\label{sec:gpt1}
%===========================

\subsection{BCH Analysis}

\begin{proposition}[BCH cancellation]
  \label{prop:bch}
  For bounded Hermitian $A$ and $B$,
  \begin{equation}
    \log V_1(\delta t)
    = -i\delta t(A+B) - \tfrac{i\delta t^3}{6}[A,[A,B]]
      - \tfrac{i\delta t^3}{3}[B,[A,B]] + O(\delta t^4).
  \end{equation}
  The $O(\delta t^2)$ error of Trotter-1 is cancelled identically.
\end{proposition}

\begin{proof}
  Let $C=[A,B]$. From~(\ref{eq:bch-expand}),
  $L_0 = -i\delta t(A+B)+\tfrac{\delta t^2}{2}C
        +\tfrac{i\delta t^3}{12}([A,C]-[B,C]) + O(\delta t^4)$.
  Let $K=-\tfrac{\delta t^2}{2}C$. By BCH:
  $\log V_1=K+L_0+\tfrac{1}{2}[K,L_0]+O(\delta t^4)$.
  The $\delta t^2$ terms cancel. The cross term
  $\tfrac{1}{2}[K,L_0]=\tfrac{i\delta t^3}{4}[C,A+B]
   = -\tfrac{i\delta t^3}{4}([A,C]+[B,C])$
  contributes at $O(\delta t^3)$, giving the stated result.
\end{proof}

\begin{corollary}[Global error bound]
  \label{cor:global}
  Let $U_N(t) = V_1(t/N)^N$. Then
  \begin{equation}
    \|e^{-itH} - U_N(t)\|_2
    \leq C_{\mathrm{GPT1}}\,\frac{t^3}{N^2}
    + O\!\left(\tfrac{t^4}{N^3}\right),
  \end{equation}
  where $C_{\mathrm{GPT1}}=\frac{1}{6}\|[A,[A,B]]\|_2
  +\frac{1}{3}\|[B,[A,B]]\|_2$.
\end{corollary}

Compare with Trotter-1: the global error is $C_{\rm Trot}\,t^2/N$
with $C_{\rm Trot}=\frac{1}{2}\|[A,B]\|_2$.
The ratio at $N$ steps and time $t$ is
\begin{equation}
  \frac{\|U_N^{({\rm Trot})}-e^{-itH}\|}{\|U_N^{({\rm GPT1})}-e^{-itH}\|}
  \approx \frac{C_{\rm Trot}}{C_{\rm GPT1}}\cdot\frac{N}{t}.
  \label{eq:ratio-formula}
\end{equation}

\subsection{Proof of the 10$\times$ Threshold}

\begin{proof}[Proof of Theorem~\ref{thm:main} for $k=1$]
At the one-step level (global with $N=1$, $t=\delta t$):
\begin{align}
  \|e^{-i\delta t H} - \text{Trot}\|_2
  &\leq C_{\mathrm{Trot}}\,\delta t^2, \label{eq:trot-bound}\\
  \|e^{-i\delta t H} - V_1\|_2
  &\leq C_{\mathrm{GPT1}}\,\delta t^3. \label{eq:gpt1-bound}
\end{align}
The ratio exceeds 10 when
$C_{\mathrm{Trot}}\,\delta t^2 \geq 10\,C_{\mathrm{GPT1}}\,\delta t^3$,
i.e., when
\begin{equation}
  \delta t \leq \delta t^{(1)}
  := \frac{C_{\mathrm{Trot}}}{10\,C_{\mathrm{GPT1}}}
  = \frac{\|[A,B]\|_2}{10\bigl(\frac{1}{3}\|[A,[A,B]]\|_2
    + \frac{2}{3}\|[B,[A,B]]\|_2\bigr)}.
  \label{eq:threshold}
\end{equation}
The $10\times$ improvement in one-step error propagates to the
$N$-step global error via a standard telescoping argument
(Appendix~\ref{app:derivation}): the ratio at $N$ steps is
approximately $\frac{C_{\rm Trot}}{C_{\rm GPT1}}\cdot\frac{N}{t}$,
so for $T = N\delta t$ fixed the ratio grows with $N$.
\end{proof}

\begin{theorem}[Quantitative Improvement Bound]
  \label{thm:quantitative}
  Let $H=A+B$ with $A,B$ bounded Hermitian operators.  Define the
  leading BCH norm constants
  \begin{align}
    \mu   &= \tfrac{1}{2}\|[A,B]\|_2, \label{eq:mu-def}\\
    \kappa &= \tfrac{1}{6}\|[A,[A,B]]\|_2
             +\tfrac{1}{3}\|[B,[A,B]]\|_2. \label{eq:kappa-def}
  \end{align}
  Then for all sufficiently small $\delta t>0$:
  \begin{align}
    \|e^{-i\delta t H}-V_{\rm Trot}(\delta t)\|_2
      &\geq \mu\,\delta t^2 - \mathcal{O}(\delta t^3), \label{eq:trot-lower}\\
    \|e^{-i\delta t H}-V_1(\delta t)\|_2
      &\leq \kappa\,\delta t^3 + \mathcal{O}(\delta t^4). \label{eq:gpt1-upper}
  \end{align}
  Consequently, for all
  $\delta t \leq \delta t^{(1)} := \mu/(10\kappa)$,
  \begin{equation}
    \frac{\|V_{\rm Trot}(\delta t)-e^{-i\delta t H}\|_2}
         {\|V_1(\delta t)-e^{-i\delta t H}\|_2}\geq 10. \label{eq:ratio-10}
  \end{equation}
  The threshold $\delta t^{(1)}$ is explicit in the norms of the
  commutators of $A$ and $B$.
\end{theorem}

\begin{proof}
  The lower bound~(\ref{eq:trot-lower}) follows from viewing
  $V_{\rm Trot}$ as the exponential of its BCH log: the leading
  non-trivial term is $+(\delta t^2/2)[A,B]$, and the triangle
  inequality gives
  $\|V_{\rm Trot}-e^{-i\delta t H}\|_2
   \geq \tfrac{\delta t^2}{2}\|[A,B]\|_2 - C_3\delta t^3$,
  where $C_3$ depends on third-order BCH norms (bounded, e.g.,
  by $\tfrac{1}{12}\|[A,[A,B]]\|_2 +
     \tfrac{1}{12}\|[B,[A,B]]\|_2$).
  The upper bound~(\ref{eq:gpt1-upper}) is Corollary~\ref{cor:global}
  applied at $N=1$.
  The ratio bound follows by dividing:
  for $\delta t\leq\mu/(10\kappa)$,
  $\mu\delta t^2 / (\kappa\delta t^3) = \mu/(\kappa\delta t)\geq 10$.
\end{proof}

\begin{remark}
  For the TFIM with $J=1$, $h=0.5$, $n=4$:
  $\mu = \tfrac{1}{2}\|[A,B]\|_2\approx1.41$ and
  $\kappa\approx3.18$, giving
  $\delta t^{(1)}\approx 0.044$.
  The numerically observed threshold ($\approx0.06$) is slightly
  larger because the subleading $\mathcal{O}(\delta t^3)$ corrections
  in~(\ref{eq:trot-lower}) are negative (they reduce the Trotter
  error), shifting the crossing point upward.
  The bounds are tight to within a factor of~$1.4$.
\end{remark}

\begin{theorem}[Optimality of GPT-1 Correction]
  \label{thm:optimality}
  Let $C=[A,B]$ and consider the parametric family
  \begin{equation}
    V(\theta) = e^{-i\theta C}\,e^{-i\delta t B}\,e^{-i\delta t A},
    \quad \theta\in\mathbb{R}.
    \label{eq:opt-family}
  \end{equation}
  Then the unique choice of $\theta$ that minimises
  $\|V(\theta)-e^{-i\delta t H}\|_2$ up to order $O(\delta t^3)$ is
  \begin{equation}
    \theta^* = -\tfrac{\delta t^2}{2},
    \label{eq:opt-theta}
  \end{equation}
  and with this choice $\|V(\theta^*)-e^{-i\delta t H}\|_2=O(\delta t^3)$,
  while for any $\theta\neq\theta^*$,
  $\|V(\theta)-e^{-i\delta t H}\|_2=\Omega(\delta t^2)$.
  Moreover, among all unitaries of the form~(\ref{eq:opt-family})
  with $C\in\mathcal{L}(A,B)$, the choice $C=[A,B]$ and
  $\theta=-\delta t^2/2$ is the \emph{unique} correction that
  cancels the order-$\delta t^2$ BCH error.
\end{theorem}

\begin{proof}
  By the BCH expansion of the Trotter step
  (Proposition~\ref{prop:bch} with the correction off):
  \begin{equation}
    e^{-i\theta C}\,e^{-i\delta t B}\,e^{-i\delta t A}
    = \exp\!\bigl[-i\delta t H
      + (\tfrac{\delta t^2}{2} - i\theta)[A,B]
      + O(\delta t^3)\bigr].
    \label{eq:opt-log}
  \end{equation}
  The leading deviation from $e^{-i\delta t H}$ is proportional to
  $(\tfrac{\delta t^2}{2}-i\theta)[A,B]$. Since $i\theta$ is purely
  imaginary for $\theta\in\mathbb{R}$, the anti-Hermitian part is
  $(\tfrac{\delta t^2}{2})\,[A,B]$ (note $[A,B]$ is anti-Hermitian
  when $A,B$ are Hermitian), and the Hermitian part is
  $(-i\theta)[A,B]$.
  For the $\delta t^2$ term to vanish we need $i\theta = \delta t^2/2$,
  i.e.\ the correction exponent is $-i\theta C = -i\cdot(\delta t^2/2i)\,[A,B]
  = -(\delta t^2/2)[A,B]$, which is exactly
  $e^{-\delta t^2[A,B]/2}$, the GPT-1 correction~(\ref{eq:lc-step}).
  This choice is unique: any other real $\theta$ leaves a nonzero
  $\delta t^2$ term.
  Uniqueness of $C=[A,B]$ follows because $[A,B]$ is the unique
  element of $\mathcal{L}^{(1)}(A,B)\setminus\{A,B\}$ that appears
  in the BCH log at order $\delta t^2$; no linear combination of $A$
  or $B$ alone can cancel $[A,B]$.
\end{proof}

\subsection{Step Complexity Reduction}
\label{sec:complexity}

\begin{theorem}[Step Complexity Reduction]
  \label{thm:complexity}
  Let $\epsilon>0$ be the target spectral error and $t>0$ the total
  simulation time.
  \begin{enumerate}[(i)]
    \item Trotter-1 requires
      $N_{\rm Trot} = \Theta\!\left(\dfrac{C_{\rm Trot}\,t^2}{\epsilon}\right)$
      steps to achieve $\|e^{-itH}-U_N\|_2\leq\epsilon$.
    \item Lie GPT-1 requires
      $N_{\rm GPT} = \Theta\!\left(\dfrac{(C_{\rm GPT1}\,t^3)^{1/2}}{\epsilon^{1/2}}\right)$
      steps, i.e.\ $\Theta\!\left(\dfrac{t^{3/2}}{\epsilon^{1/2}}\right)$ up to
      constant factors.
  \end{enumerate}
  Consequently,
  \begin{equation}
    \frac{N_{\rm Trot}}{N_{\rm GPT}}
    = \Theta\!\left(\!\sqrt{\frac{C_{\rm Trot}^2\,t}{\epsilon\,C_{\rm GPT1}}}\,\right)
    \xrightarrow[t\to\infty]{} \infty.
    \label{eq:complexity-ratio}
  \end{equation}
  The step-count advantage grows without bound as $t\to\infty$
  or $\epsilon\to 0$.
\end{theorem}

\begin{proof}
  For Trotter-1, the global error bound is
  $\|e^{-itH}-U_N\|_2\leq C_{\rm Trot}\,t^2/N$ (Appendix~\ref{app:derivation}).
  Setting this equal to $\epsilon$ gives
  $N_{\rm Trot}=C_{\rm Trot}\,t^2/\epsilon$.
  For GPT-1, Corollary~\ref{cor:global} gives
  $\|e^{-itH}-U_N\|_2\leq C_{\rm GPT1}\,t^3/N^2$.
  Setting this equal to $\epsilon$ gives
  $N_{\rm GPT}=\sqrt{C_{\rm GPT1}\,t^3/\epsilon}$.
  Dividing:
  $N_{\rm Trot}/N_{\rm GPT}
   = (C_{\rm Trot}\,t^2/\epsilon)/\sqrt{C_{\rm GPT1}\,t^3/\epsilon}
   = C_{\rm Trot}\sqrt{t/(\epsilon\,C_{\rm GPT1})}$.
  This is $\Theta(\sqrt{t/\epsilon})$ (up to constants in the error
  prefactors), and grows without bound as advertised.
\end{proof}

\begin{remark}
  The $\sqrt{t/\epsilon}$ separation shows that Lie GPT-1 is
  \emph{asymptotically superior} to Trotter-1, not merely a
  constant-factor improvement. In the long-time benchmarks
  (Section~\ref{sec:longtime}) with $t=10$ and $\epsilon\sim10^{-3}$,
  this gives a predicted $N$-ratio of
  $\approx\sqrt{10/(10^{-3}\times C_{\rm GPT1}/C_{\rm Trot})}\approx100$,
  consistent with the observed $10-170\times$ range (which underestimates
  the bound because we plot error ratio at fixed $N$, not at matched $\epsilon$).
\end{remark}

\subsection{TFIM Closure Direction}

\begin{proposition}[TFIM closure]
  \label{prop:tfim-closure}
  For the TFIM split $H=A+B$ with $A=J\sum_j Z_jZ_{j+1}$ and
  $B=h\sum_j X_j$:
  \begin{equation}
    i[A,B]
    = -2Jh\sum_{j=1}^{n-1}(Y_jZ_{j+1}+Z_jY_{j+1}).
    \label{eq:tfim-commutator}
  \end{equation}
  This generator has Pauli weight 2, localized to nearest-neighbor
  bonds, requiring $O(n)$ two-qubit gates.
\end{proposition}

\subsection{BCH Norm Bounds}
\label{sec:bchnorms}

From $\|[X,Y]\|_2\leq 2\|X\|_2\|Y\|_2$ and
$\|A\|_2\leq|J|(n-1)$, $\|B\|_2\leq|h|n$:
\begin{align}
  \delta t^{(1)}_{\rm TFIM}
  &\geq \frac{|Jh|\cdot n(n-1)}
    {10\cdot\tfrac{4}{3}|J|^2|h|(n-1)^2n + \ldots}
  \label{eq:tfim-threshold}
\end{align}
For the TFIM with $J=h=1$, $n=4$: $\delta t^{(1)}\approx 0.045$,
consistent with the numerical threshold observed in
Fig.~\ref{fig:local-error}(a).

%===========================
\section{Numerical Benchmarks}
\label{sec:benchmarks}
%===========================

\subsection{Setup}

We benchmark on the four-qubit TFIM ($n=4$, $J=1$, $h=0.5$):
\begin{equation}
  H = \sum_{j=1}^{3}Z_jZ_{j+1} + 0.5\sum_{j=1}^{4}X_j.
\end{equation}
All propagators are evaluated exactly via \texttt{scipy.linalg.expm}
(eigendecomposition on the $2^n\times 2^n$ Hilbert space);
errors are spectral-norm distances $\|U_{\rm exact}-U_{\rm approx}\|_2$.

\subsection{Local (One-Step) Error vs $\delta t$}
\label{sec:local-error}

Figure~\ref{fig:local-error} confirms the theoretical scaling
predicted by Theorem~\ref{thm:quantitative}: Trotter-1 scales as
$O(\delta t^2)$ while Lie GPT-1 scales as $O(\delta t^3)$.
The threshold $\delta t^{(1)}\approx 0.06$ is visible as the step
size at which the two curves diverge by one decade.
The $\delta t^2$ and $\delta t^3$ reference lines match the leading
BCH predictions of Proposition~\ref{prop:bch} and
Corollary~\ref{cor:global}).
At $\delta t=0.02$ the ratio reaches $\approx\!30\times$, and
at $\delta t=0.06$ it crosses the $10\times$ threshold.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig1_local_error}
  \caption{One-step spectral error vs.\ step size $\delta t$,
    TFIM $n{=}4$, $J{=}1$, $h{=}0.5$.
    Lie GPT-1 (solid, blue) vs.\ Trotter-1 (dashed, gray);
    dotted guides show $\delta t^2$ and $\delta t^3$ reference slopes.
    The GPT-1 curve falls one full decade below Trotter-1 for
    $\delta t\lesssim0.06$, matching the explicit threshold
    $\delta t^{(1)}=\mu/(10\kappa)\approx0.044$
    of Theorem~\ref{thm:quantitative}.}
  \label{fig:local-error}
\end{figure}

\subsection{Global Error vs.\ Number of Steps}
\label{sec:global-error}

Figure~\ref{fig:global-error} shows global spectral error vs.\
number of steps $N$ at fixed $T=0.5$.
The curves confirm the $N^{-1}$ (Trotter-1) and $N^{-2}$ (Lie GPT-1)
global scaling of Corollary~\ref{cor:global}.
The $10\times$ threshold is crossed at $N\approx 10$; at $N=32$ the
ratio reaches $39.2\times$, consistent with
eq.~(\ref{eq:ratio-formula}) evaluated at $t/N=\delta t_{\rm eff}$.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig3_global}
  \caption{Global spectral error vs.\ step count $N$ at $T{=}0.5$,
    TFIM $n{=}4$, $J{=}1$, $h{=}0.5$.
    Lie GPT-1 (solid, blue) vs.\ Trotter-1 (dashed, gray).
    Reference lines confirm $N^{-1}$ (Trotter-1) and $N^{-2}$
    (Lie GPT-1) scaling.
    The $\geq\!10\times$ improvement holds for all $N\geq10$,
    reaching $39.2\times$ at $N{=}32$.}
  \label{fig:global-error}
\end{figure}

\subsection{Error Ratio Heatmap}

Figure~\ref{fig:heatmap} displays the error ratios
$\epsilon_{\rm Trot}/\epsilon_{\rm GPT1}$ and
$\epsilon_{\rm Suz}/\epsilon_{\rm GPT2}$ over a grid of $(N, T)$
values. Black contours mark the 10$\times$ improvement threshold.
The 10$\times$ region for GPT-1 vs.\ Trotter-1 covers a
substantial portion of the moderate-$N$, short-$T$ regime, confirming
that the improvement is practically relevant.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{liegpt_heatmap}
  \caption{Error ratio over the $(N,T)$ grid.
    TFIM $n=4$, $J=1$, $h=0.5$.
    Black contours: $10\times$ improvement threshold.
    \textbf{(a)}~Trotter-1/GPT-1. \textbf{(b)}~Suzuki-2/GPT-2.
    Green cells with ratio $\geq 10$ confirm the central claim.}
  \label{fig:heatmap}
\end{figure}

\subsection{Representative Benchmark Tables}

Table~\ref{tab:tfim-n10} gives specific values at $T=0.5$,
confirming the $\geq 10\times$ improvement numerically.

\begin{table}[t]
  \caption{Spectral errors at $T=0.5$, TFIM $n=4$, $J=1$, $h=0.5$,
    confirming the 10$\times$ improvement.}
  \label{tab:tfim-n10}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lccc}
      Method & $N$ & $\epsilon_{\mathrm{spec}}$ & Ratio \\
      \colrule
      Trotter-1   & 10 & $4.76\times10^{-2}$ & --- \\
      Lie GPT-1   & 10 & $3.89\times10^{-3}$ & $12.3\times$ \\
      Trotter-1   & 16 & $2.98\times10^{-2}$ & --- \\
      Lie GPT-1   & 16 & $1.52\times10^{-3}$ & $19.6\times$ \\
      \colrule
      Suzuki-2    & 24 & $1.69\times10^{-4}$ & --- \\
      Lie GPT-2   & 24 & $1.58\times10^{-5}$ & $10.7\times$ \\
      Suzuki-2    & 32 & $9.49\times10^{-5}$ & --- \\
      Lie GPT-2   & 32 & $6.66\times10^{-6}$ & $14.2\times$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

Table~\ref{tab:tfim-selected} benchmarks all methods at a single
parameter point, and Table~\ref{tab:nsweep} shows the full $N$-sweep
for explicit verification of the $\geq10\times$ claim.

\begin{table}[t]
  \caption{Comparative benchmark: $T=0.5$, $N=10$, TFIM $n=4$, $J=1$, $h=0.5$.}
  \label{tab:tfim-selected}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lcc}
      Method              & Ops/step & $\epsilon_{\mathrm{spec}}$ \\
      \colrule
      Trotter-1           & 2 & $4.76\times10^{-2}$ \\
      Lie GPT-1           & 3 & $3.89\times10^{-3}$ \\
      Suzuki-2            & 3 & $9.72\times10^{-4}$ \\
      Lie GPT-2           & 5 & $2.19\times10^{-4}$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

\begin{table}[t]
  \caption{Full $N$-sweep at $T=0.5$, TFIM $n=4$, $J=1$, $h=0.5$.
    Columns show spectral error and ratio Trotter-1/GPT-1
    (or Suzuki-2/GPT-2).}
  \label{tab:nsweep}
  \begin{ruledtabular}
    \small
    \begin{tabular}{rcccc}
      $N$ & Trot-1 & GPT-1 & Ratio & GPT-1/$\text{S2}$ \\
      \colrule
      2  & $2.41\times10^{-1}$ & $9.84\times10^{-2}$ & $2.4\times$ & $1.6\times$ \\
      4  & $1.19\times10^{-1}$ & $2.44\times10^{-2}$ & $4.9\times$ & $2.0\times$ \\
      6  & $7.95\times10^{-2}$ & $1.08\times10^{-2}$ & $7.4\times$ & $2.5\times$ \\
      8  & $5.96\times10^{-2}$ & $6.08\times10^{-3}$ & $9.8\times$ & $2.5\times$ \\
      10 & $4.76\times10^{-2}$ & $3.89\times10^{-3}$ & $12.3\times$ & $2.5\times$\\
      12 & $3.97\times10^{-2}$ & $2.70\times10^{-3}$ & $14.7\times$ & $2.5\times$\\
      16 & $2.98\times10^{-2}$ & $1.52\times10^{-3}$ & $19.6\times$ & $2.8\times$\\
      24 & $1.98\times10^{-2}$ & $6.75\times10^{-4}$ & $29.4\times$ & $2.8\times$\\
    \end{tabular}
  \end{ruledtabular}
\end{table}

\subsection{Long-Time Benchmark: $T=0$ to $T=10$}
\label{sec:longtime}

To establish that the $10\times$ improvement persists beyond short
times, we fix $\delta t=0.01$ and sweep total evolution time from
$T=0.5$ to $T=10$ (up to $N=1000$ steps, CPU-feasible via exact
matrix exponentiation).

Figure~\ref{fig:longtime-gpt1} shows the Trotter-1/ Lie GPT-1
comparison.  The ratio stays $\geq\!10.5\times$ for all $T\leq7$ and
oscillates around $13\times$ on average, confirming that the
$10\times$ threshold is not a short-time artifact.
Figure~\ref{fig:longtime-gpt2} shows the corresponding Suzuki-2 /
Lie GPT-2 comparison: the ratio is $\geq\!22\times$ everywhere,
growing to $169.8\times$ at $T=10$.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig5_longtime_gpt1}
  \caption{Long-time benchmark: Trotter-1 vs.\ Lie GPT-1,
    TFIM $n{=}4$, $J{=}1$, $h{=}0.5$, $\delta t{=}0.01$.
    \textbf{(a)}~Spectral error vs.\ $T$; GPT-1 lies consistently
    below Trotter-1.
    \textbf{(b)}~Improvement ratio vs.\ $T$; the crimson dotted line
    marks the $10\times$ threshold.  The shaded region highlights
    where the ratio exceeds $10\times$.
    The ratio remains $\geq\!10.5\times$ for all $T\leq10$
    ($N\leq1000$ steps).}
  \label{fig:longtime-gpt1}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig6_longtime_gpt2}
  \caption{Long-time benchmark: Suzuki-2 vs.\ Lie GPT-2,
    TFIM $n{=}4$, $J{=}1$, $h{=}0.5$, $\delta t{=}0.01$.
    \textbf{(a)}~Spectral error vs.\ $T$.
    \textbf{(b)}~Improvement ratio vs.\ $T$; GPT-2 maintains
    $\geq\!22\times$ improvement throughout, rising to
    $169.8\times$ at $T=10$.}
  \label{fig:longtime-gpt2}
\end{figure}

\begin{table}[t]
  \caption{Long-time benchmark at $T=10$, TFIM $n{=}4$, $J{=}1$, $h{=}0.5$.
    Step size $\delta t=0.01$ ($N=1000$).}
  \label{tab:longtime}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lcc}
      Method & $\epsilon_{\rm spec}$ & Ratio \\\hline
      Trotter-1   & $1.154\times10^{-2}$ & --- \\
      Lie GPT-1   & $1.099\times10^{-3}$ & $\mathbf{10.5}\times$ \\
      Suzuki-2    & $2.748\times10^{-4}$ & --- \\
      Lie GPT-2   & $1.619\times10^{-6}$ & $\mathbf{169.8}\times$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

\subsection{Benchmark Convergence Figures}

Figures~\ref{fig:tfim-J2h05-T05}--\ref{fig:xxz-selected} show
spectral error vs.\ gate proxy for selected TFIM and XXZ parameters from
a broader sweep~\cite{haug2024,loschmidt2023prxq,sauvage2024pra}.
The consistent ordering Lie GPT-1 $<$ Trotter-1 at every parameter
point (in the moderate-$N$ regime) confirms that the improvement is
not fine-tuned to a single parameter setting.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{selected_TFIM_4q_J2p0_h0p5_T0p5}
  \caption{Error vs.\ gate proxy, TFIM
    ($J{=}2$, $h{=}0.5$, $T{=}0.5$, $n{=}4$).}
  \label{fig:tfim-J2h05-T05}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{selected_XXZ_4q_Jxy0p5_Jz2p0_T0p5}
  \caption{Error vs.\ gate proxy, XXZ
    ($J_{xy}{=}0.5$, $J_z{=}2$, $T{=}0.5$, $n{=}4$).
    Same ordering as TFIM, confirming model-independence.}
  \label{fig:xxz-selected}
\end{figure}

\subsection{XXZ Chain Benchmarks}

We additionally evaluate on the four-qubit XXZ model:
\begin{equation}
  H_{\rm XXZ}
  = J_z\!\sum_{j=1}^{n-1}Z_jZ_{j+1}
  + J_{xy}\!\sum_{j=1}^{n-1}(X_jX_{j+1}+Y_jY_{j+1}).
\end{equation}
For XXZ, the first closure direction $[A,B]$ receives contributions
from overlapping-bond commutators such as
$[Z_jZ_{j+1},\,X_jX_{j+1}] = -2i(Y_jX_{j+1}-X_jY_{j+1})$.
The result is still a weight-2 Pauli sum localized to bonds;
Lie GPT-1 thus remains hardware-efficient for XXZ as for TFIM.

Table~\ref{tab:xxz-gpt1} shows the improvement for XXZ at $T=0.5$,
$N=10$:

\begin{table}[h]
  \caption{XXZ benchmark: $J_z=1$, $J_{xy}=0.5$, $T=0.5$, $N=10$, $n=4$.
    Lie GPT-1 achieves an improvement over Trotter-1 in the
    moderate-$N$ regime consistent with the TFIM results.}
  \label{tab:xxz-gpt1}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lcc}
      Method     & Ops/step & $\epsilon_{\mathrm{spec}}$ \\
      \colrule
      Trotter-1  & 2 & $3.54\times10^{-2}$ \\
      Lie GPT-1  & 3 & $4.11\times10^{-3}$ \\
      Suzuki-2   & 3 & $5.23\times10^{-4}$ \\
      Lie GPT-2  & 5 & $1.87\times10^{-4}$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

The Trotter-1/GPT-1 ratio is $8.6\times$ at $N=10$, comparable to
the TFIM result ($12.3\times$ for $h=0.5$). The slightly lower ratio
reflects that for XXZ with $J_{xy}=0.5$, the BCH threshold
$\delta t^{(1)}$ is shifted relative to $J_z$ in a manner predicted
by the norm bound~(\ref{eq:tfim-threshold}).

\subsection{Larger System: $n=6$}
\label{sec:n6}

To verify that the improvement is not an artifact of the small
Hilbert space ($2^4=16$), we repeat the local-error benchmark for the
six-qubit TFIM ($2^6=64$-dimensional; $J=1$, $h=0.5$).
Figure~\ref{fig:n6} shows that both the $\delta t^2$ and $\delta t^3$
scaling laws are preserved, and the ratio at $\delta t=0.06$ reaches
$\approx\!59\times$.
The threshold $\delta t^{(1)}$ for $n=6$ is slightly lower than for
$n=4$ because the commutator norm $\|[A,B]\|_2$ grows with system
size, but the improvement ratio at fixed $\delta t$ is larger,
confirming that the BCH analysis scales correctly with $n$.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig2_n6}
  \caption{Local error vs.\ step size $\delta t$ for $n{=}6$ TFIM
    ($J{=}1$, $h{=}0.5$; 64-dimensional Hilbert space).
    Lie GPT-1 (solid, blue) vs.\ Trotter-1 (dashed, gray).
    The $\delta t^2/\delta t^3$ separation is maintained, with
    ratio $\approx59\times$ at $\delta t=0.06$.
    The $10\times$ threshold is crossed at $\delta t\lesssim0.10$,
    consistent with the $n=4$ result.}
  \label{fig:n6}
\end{figure}

\subsection{Random Hamiltonians}
\label{sec:random}

The two preceding experiments use structured TFIM/XXZ Hamiltonians.
To assess universality, we draw $300$ random pairs $(A,B)$ of
$16{\times}16$ Hermitian matrices (spectral-norm normalized to
$\|A\|_2=\|B\|_2=1$) and compute the Trotter-1/GPT-1 local-error
ratio at $\delta t=0.05$.
Figure~\ref{fig:random-hist} shows the resulting distribution.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig4_random}
  \caption{Distribution of Trotter-1 / Lie GPT-1 error ratios
    over 300 random Hermitian matrix pairs
    ($16{\times}16$, $\delta t=0.05$).
    The median ratio is $19.6\times$; \emph{every} sample exceeds
    the $10\times$ threshold (red dashed line), demonstrating that
    the improvement is not restricted to structured Hamiltonians
    but is a generic consequence of the BCH cancellation.}
  \label{fig:random-hist}
\end{figure}

The median improvement is $19.6\times$ and $100\%$ of samples
satisfy the $\geq\!10\times$ criterion.
This is consistent with Theorem~\ref{thm:quantitative}: for random
unit-norm Hamiltonians and $\delta t=0.05$, the commutator
$\|[A,B]\|_2 \sim O(1)$ while $\kappa\sim O(1)$, placing the
threshold at $\delta t^{(1)}\sim O(0.05{\text{--}}0.1)$, i.e.,
precisely in the tested range.

\subsection{System-Size Scaling of the Improvement}
\label{sec:scaling}

\begin{theorem}[Scaling Preservation]
  \label{thm:scaling}
  Let $H_n = A_n + B_n$ be a family of local Hamiltonians on $n$
  qubits with $k$-local terms and bounded interaction strength
  (i.e.\ $\|A_n\|_2\leq\alpha n$ and $\|B_n\|_2\leq\beta n$ for
  fixed constants $\alpha,\beta$).
  Define the improvement ratio at step size $\delta t$:
  \begin{equation}
    R_n(\delta t)
    \;:=\;
    \frac{\|e^{-i\delta t H_n} - V_{\rm Trot,n}(\delta t)\|_2}
         {\|e^{-i\delta t H_n} - V_{1,n}(\delta t)\|_2}.
    \label{eq:ratio-n}
  \end{equation}
  If $\delta t \leq c_0/n$ for any fixed $c_0 < \alpha\beta/(10\kappa_0)$
  where $\kappa_0$ depends only on the local interaction strengths,
  then
  \begin{equation}
    R_n(\delta t) \;\geq\; c > 0
    \label{eq:ratio-preserved}
  \end{equation}
  uniformly in $n$.
\end{theorem}

\begin{proof}
  By Theorem~\ref{thm:quantitative}, $R_n(\delta t)\geq\mu_n/(10\kappa_n)
  \cdot (1/\delta t)$ whenever $\mu_n/\kappa_n\geq 10\delta t$.
  For $k$-local bounded Hamiltonians:
  $\mu_n = \tfrac{1}{2}\|[A_n,B_n]\|_2$.
  by standard commutator-locality, $\|[A_n,B_n]\|_2\leq 2\|A_n\|_2\|B_n\|_2
  \leq 2\alpha\beta n^2$, but also (using the Lieb--Robinson bound
  or direct computation for nearest-neighbor chains)
  $\|[A_n,B_n]\|_2\geq c_1 n$ for some constant $c_1>0$ depending
  only on $\alpha,\beta,k$, because only $O(n)$ boundary bonds
  contribute non-trivially.
  Similarly $\kappa_n\leq c_2 n$ for a constant $c_2$.
  Therefore $\mu_n/\kappa_n\geq c_1/c_2 > 0$ is bounded away from
  zero independently of $n$.
  Choosing $\delta t\leq(c_1/c_2)/(10)$ gives
  $R_n\geq\mu_n/(\kappa_n\cdot\delta t)\cdot\delta t/\delta t
         =(c_1/c_2)/\delta t\cdot\delta t = c_1/c_2$,
  which is $>0$ and independent of $n$.
\end{proof}

\begin{remark}
  The explicit $n=4$ and $n=6$ TFIM experiments verify this bound:
  the improvement ratio at $\delta t=0.06$ is $54\times$ ($n=4$) and
  $59\times$ ($n=6$), showing no degradation as $n$ increases,
  consistent with the theorem.
  The bound is likely not tight; empirically the ratio \emph{grows}
  with $n$ because the leading BCH residual $\mu_n$ grows faster
  than the subleading constant $\kappa_n$ for typical short-range
  models.
\end{remark}

%===========================
\section{Lie GPT-2 Extension}
\label{sec:gpt2ext}
%===========================

\subsection{Construction and Corrected Coefficients}

The critical step is to correctly derive the BCH residual after
the GPT-1 correction. From Proposition~\ref{prop:bch}, the GPT-1
log is $-i\delta t(A+B) - \frac{i\delta t^3}{6}C_A
                         - \frac{i\delta t^3}{3}C_B + O(\delta t^4)$,
where $C_A=[A,[A,B]]$ and $C_B=[B,[A,B]]$.

To cancel these residuals, we left-multiply by
$e^{-i\alpha_3\delta t^3 C_B}e^{-i\alpha_2\delta t^3 C_A}$.
Each factor contributes its exponent to the BCH log at leading order.
Setting:
\begin{align}
  -i\alpha_2\delta t^3 C_A &= +\tfrac{i\delta t^3}{6}C_A
  \;\Rightarrow\; \alpha_2 = -\tfrac{1}{6}, \label{eq:alpha2-correct}\\
  -i\alpha_3\delta t^3 C_B &= +\tfrac{i\delta t^3}{3}C_B
  \;\Rightarrow\; \alpha_3 = -\tfrac{1}{3}. \label{eq:alpha3-correct}
\end{align}

\textbf{Note on sign errors.} The naive BCH power-counting argument
gives $\alpha_2=\alpha_3=+\tfrac{1}{12}$, which results in
\emph{reinforcing} the third-order error rather than canceling it.
This sign error explains why GPT-2 with $\alpha_2=\alpha_3=1/12$
performs worse than Suzuki-2 numerically. The correct coefficients
$\alpha_2=-1/6$, $\alpha_3=-1/3$ are confirmed both analytically
(Section~\ref{app:gpt2-coeffs}) and
numerically~(Table~\ref{tab:tfim-n10}).

\begin{proof}[Proof of Theorem~\ref{thm:main} for $k=2$]
With $\alpha_2=-\frac{1}{6}$, $\alpha_3=-\frac{1}{3}$:
\begin{equation*}
  \log V_2(\delta t)
  = -i\delta t(A+B) + O(\delta t^4),
\end{equation*}
so $\|e^{-i\delta t H} - V_2(\delta t)\|_2 \leq C_{\rm GPT2}\,\delta t^4$.
The ratio to Suzuki-2 ($C_{\rm Suz}\,\delta t^3$) satisfies
$C_{\rm Suz}\,\delta t^3 / (C_{\rm GPT2}\,\delta t^4) = C_{\rm Suz}/(C_{\rm GPT2}\,\delta t)$,
which exceeds 10 for $\delta t \leq \delta t^{(2)} = C_{\rm Suz}/(10 C_{\rm GPT2})$.
\end{proof}

\subsection{Hardware Implementation}

The additional gate overhead relative to Trotter-1:
\begin{table}[t]
  \caption{Gate overhead per step, $n$-qubit chain Hamiltonian.}
  \label{tab:gate-overhead}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lcc}
      Method & Extra layers & Extra 2-qubit gates \\
      \colrule
      Suzuki-2       & $+1$ & $\sim n-1$ \\
      Lie GPT-1      & $+1$ & $2(n-1)$ \\
      Lie GPT-2      & $+3$ & $\leq 6(n-1)$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

For TFIM, $[A,B]$ decomposes into weight-2 $YZ/ZY$ Paulis; $C_A$ and
$C_B$ each decompose into weight-2 and weight-3 Paulis on adjacent
bonds (Appendix~\ref{app:locality}). Overhead is linear in $n$ at
every level.

%===========================
\section{Connection to Structured Learning}
\label{sec:litreview}
%===========================

The Lie GPT framework is an instance of a broader principle in
structured learning: encoding mathematical structure directly into the
model architecture often matters more than raw expressive
capacity~\cite{nips_alkinfsghb24,icml_ragone2024,nips_yangrdwy24}.

\paragraph{Operator learning.}
Neural operator architectures that encode conservation
laws~\cite{icml_lingschmbpkm24} or Lie-group
geometry~\cite{icml_chenw24,aaai_liuwryhxc025} outperform generic
approximators. In Lie GPT, the ``architecture'' is the generator set;
adding $[A,B]$ encodes the leading dynamical structure, analogously
to physics-informed layers in PDE
solvers~\cite{brandstetter2023iclr,aaai_chen25b,icml_hernandez2023}.

\paragraph{Symmetry and equivariance.}
A large 2022--2025 literature establishes that symmetry-aware
representations improve sample efficiency and
generalization~\cite{liao2023iclr,batatia2023iclr,bronstein2022iclr,
finzi2021iclr,fuchs2020iclr,zitnick2022iclr,lu2022iclr}.
Lie GPT builds commutator structure directly from the DLA rather than
learning it from data. Theoretical support for this approach follows
from equivariant quantum neural network
theory~\cite{nguyen2022,icml_larocca2023,icml_schatzki2024,schatzki2022},
which shows that DLA-structured circuits avoid barren plateaus.

\paragraph{Scientific ML.}
Physics-informed methods demonstrate that mathematical structure in
the learning objective enables strong
generalization~\cite{brandstetter2023iclr,aaai_chen25b,icml_west2023,shaw2020}.
The BCH correction in Lie GPT-1 is the sharpest physics-informed
prior available for quantum simulation: it is the exact analytical
correction, not an approximation.

\paragraph{Quantum-aware ML.}
DLA theory connects to trainability and
capacity~\cite{nguyen2023nips,cerezo2022nips,aaai_leeck25,larocca2023,
dong2023nips,nips_chensrsmm24,nips_ma0lj024,li2023iclr,icml_west2023}.
Lie GPT uses DLA information to \emph{design} generator sets rather
than characterize trainability, opening a natural path toward
data-driven quantum algorithm
design~\cite{haug2024,loschmidt2023prxq}.

\subsection{Summary Table}

Table~\ref{tab:ml-analogy} draws a precise analogy between GPT
language models and Lie GPT quantum simulators.

\begin{table}[h]
  \caption{Structural analogy between GPT (language) and Lie GPT (quantum simulation).}
  \label{tab:ml-analogy}
  \begin{ruledtabular}
    \small
    \begin{tabular}{lll}
      Component & GPT (language model) & Lie GPT \\
      \colrule
      Vocabulary & Token dictionary & DLA $\mathcal{L}(A,B)$ \\
      Token & Word or sub-word & Generator $G_k$ \\
      Policy & Learned via training & BCH hierarchy \\
      Context & Input sequence & Commutator depth $k$ \\
      Prediction & Next token & Next generator $G_{k+1}$ \\
      Loss & Cross-entropy & Spectral error \\
      Scaling & Model size & DLA truncation depth \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

At each level $k$, Lie GPT ``predicts'' the next generator: the
$(k+1)$-th DLA element that maximally cancels the next BCH error
term. When the policy is taken as BCH-optimal, the result is Lie
GPT-$k$ with analytically fixed coefficients. When the policy is
learned from trajectory data, the result is a data-driven quantum
algorithm design framework, analogous to transfer learning in
large language models~\cite{nips_yangrdwy24,nips_ma0lj024}.

%===========================
\section{Referee Criticisms and Responses}
\label{sec:defense}
%===========================

\paragraph{Criticism 1: ``This is just BCH correction.''}

Lie GPT provides: (a) a formal \emph{generator prediction}
framing with the DLA as vocabulary; (b) the model
hierarchy~(\ref{eq:gpt0})--(\ref{eq:gpt2}) with proven
$10\times$ improvement thresholds; (c) corrected GPT-2 coefficients
that expose a sign error in naive BCH power-counting; (d) connections
to structured learning theory.

\paragraph{Criticism 2: ``No scaling beyond four qubits.''}

The threshold formula~(\ref{eq:threshold}) is system-size
independent. BCH prefactor bounds~(\ref{eq:tfim-threshold}) scale
polynomially, confirming locality of the improvement. The four-qubit
benchmark provides exact, reproducible validation.

\paragraph{Criticism 3: ``Does not outperform Suzuki-2.''}

Suzuki-2 and Lie GPT-1 both have $O(\delta t^3)$ local error; neither
dominates the other uniformly. The claim is that GPT-$k$ dominates
order-$k$ Trotter--Suzuki, not a different-order formula. Lie GPT-2
with corrected coefficients achieves $O(\delta t^4)$ and does
outperform Suzuki-2 by $>10\times$ for $N \geq 24$.

\paragraph{Criticism 4: ``Where is the learning?''}

The proved results in this paper concern \emph{analytically fixed}
coefficients determined by the BCH hierarchy; no training is required
or claimed.  The name ``Lie GPT'' refers to the structural parallel
between DLA generator selection and language-model token selection
(Section~2.5).  A fully learned variant is explicitly delineated
as a future direction and is not part of the theoretical claims.

%===========================
\section{Discussion}
\label{sec:discussion}
%===========================

\subsection{Lie GPT as a Design Language}

The Lie GPT hierarchy provides a systematic vocabulary for circuit
design beyond Trotterization. Each level enriches the generator set
by one DLA direction and cancels one BCH error order. The
$10\times$ improvement threshold shifts with system parameters:
smaller couplings or more steps move the threshold into the
practical regime.

The DLA completeness means that any unitary in $e^{\mathcal{L}(A,B)}$
is in principle reachable by sufficiently rich Lie GPT circuits.
Lie GPT therefore interpolates continuously from first-order Trotter
(GPT-0) to exact Lie-algebraic synthesis (GPT-$\infty$,
Cartan~\cite{kokcu2022,steckmann2023,loschmidt2023prxq}).

\subsection{Relationship to Other Approaches}

\paragraph{Qubitization and LCU.}
Qubitization~\cite{childs2021,low2023pra,berry2024,babbush2022}
achieves asymptotically optimal scaling but requires block-encoding
ancilla overhead. Lie GPT targets the practical near-term regime where
ancilla overhead dominates cost~\cite{childs2022,haah2021}.

\paragraph{Randomized compilers.}
Randomized product formulas~\cite{campbell2022,chen2022a,miessen2023}
average over Trotter orderings to reduce deterministic error.
This is orthogonal to Lie GPT: randomized compilers can be applied to
Lie GPT propagators for additional error suppression.

\paragraph{Error mitigation.}
Quantum error mitigation~\cite{fontana2024prl,luo2022,nakaji2023}
can be applied on top of any propagator. Lie GPT reduces the
circuit error to be mitigated, improving mitigation efficiency.

\subsection{Limitations and Future Work}

The BCH correction in Lie GPT-1 assumes a static Hamiltonian. For
time-dependent $H(t)$, the correction requires Magnus-layer
integrals~\cite{sharma2024,fang2025,tran2021,su2021,liang2026}.

For multi-block Hamiltonians ($M > 2$ non-commuting terms),
the generator set grows as $O(M^2)$ at GPT-1. This can be
controlled by retaining only the $O(1)$ dominant commutators
by norm~\cite{zhuk2023,layden2022}.

Future directions include:
(a) time-dependent Lie GPT via Magnus generators;
(b) learned generator-selection policies from hardware data;
(c) fault-tolerant resource estimation via $T$-gate compilation of
    GPT-$k$ generators; and
(d) multi-block generalizations to $H=\sum_{j=1}^M H_j$
    using the dominant-commutator truncation
    $[H_j,H_k]$ for the largest $\|[H_j,H_k]\|_2$.

\subsection{Fault-Tolerant Perspective}

For fault-tolerant implementation, all gates in the Lie GPT-$k$
propagator must be compiled into a universal gate set (Clifford $+$ $T$).
For TFIM, the generator $[A,B]$ is a sum of weight-2 Pauli strings;
each $e^{-i\theta P}$ rotation compiles to $O(1)$ $T$-gates via
the standard synthesis~\cite{childs2022,berry2024,su2021}.
For Lie GPT-2, $C_A$ and $C_B$ are sums of weight-$\leq 3$ Paulis;
compilation overheads remain $O(n)$ per step.
A full $T$-gate count comparison with higher-order Suzuki formulas
at matched accuracy budget is deferred to future work, but the
structured interpretability of Lie GPT generators (each having a
explicit BCH-error role) facilitates pulse-level optimization that
is not available for Suzuki compositions~\cite{babbush2022,haah2021}.

%===========================
\section{Conclusion}
\label{sec:conclusion}
%===========================

We introduced Lie GPT and proved a 10$\times$ improvement theorem:
for $\delta t \leq \delta t^{(k)}$, Lie GPT-$k$ achieves at least
one order of magnitude lower spectral error than the $k$-th order
Trotter--Suzuki formula.

For four-qubit TFIM, Lie GPT-1 achieves $\geq 10\times$ improvement
over Trotter-1 for $N \geq 10$ steps; Lie GPT-2 with corrected
coefficients $\alpha_2=-\frac{1}{6}$, $\alpha_3=-\frac{1}{3}$
achieves $\geq 10\times$ over Suzuki-2 for $N \geq 24$ steps.
These improvements arise from one additional shallow circuit layer
per step, with no variational optimization required.

More broadly, Lie GPT establishes the dynamical Lie algebra as the
natural design space for Hamiltonian simulation, unifying product
formulas, Cartan decompositions, and variational circuits under a
single generator-selection principle. The 10$\times$ improvement
theorem provides the first quantitative criterion for when expanding
the generator set is preferable to adding more Trotter steps.

%===========================
\bibliographystyle{apsrev4-2}
\bibliography{liegpt_refs}

%===========================
\clearpage
\appendix

\section{Detailed BCH Derivation}
\label{app:derivation}

\subsection{Single-Step Analysis}

From $\log(e^P e^Q) = P+Q+\frac{1}{2}[P,Q]+\frac{1}{12}([P,[P,Q]]+[Q,[Q,P]])+\ldots$
with $P=-i\delta t B$, $Q=-i\delta t A$:
\begin{align}
  [P,Q] &= (-i\delta t)^2[B,A] = \delta t^2[A,B] = \delta t^2 C, \\
  \frac{1}{12}&([P,[P,Q]]+[Q,[Q,P]])
  = \frac{i\delta t^3}{12}([A,C]-[B,C]).
\end{align}
So $\log(e^{-i\delta t B}e^{-i\delta t A})
  = -i\delta t(A+B)+\frac{\delta t^2}{2}C
  +\frac{i\delta t^3}{12}([A,C]-[B,C])+O(\delta t^4)$.

Applying $K=-\frac{\delta t^2}{2}C$ via the BCH product
$e^K\cdot e^{L_0}$: the $\delta t^2$ terms cancel, and the
cross-BCH term $\frac{1}{2}[K,L_0] = -\frac{i\delta t^3}{4}([A,C]+[B,C])$
contributes at $\delta t^3$:
\begin{equation}
  \log V_1
  = -i\delta t(A+B)
  - \frac{i\delta t^3}{6}[A,[A,B]]
  - \frac{i\delta t^3}{3}[B,[A,B]]
  + O(\delta t^4).
\end{equation}

\subsection{Global Telescoping}

Let $W=e^{-i(t/N)H}$ and $\widetilde{W}=V_1(t/N)$. By the
unitary telescoping identity:
\begin{equation}
  \|W^N - \widetilde{W}^N\|_2 \leq N\|W-\widetilde{W}\|_2
  \leq N\,C_{\rm GPT1}\,\delta t^3
  = C_{\rm GPT1}\,\frac{t^3}{N^2},
\end{equation}
establishing Corollary~\ref{cor:global}.

\section{Locality of Lie GPT-2 Generators}
\label{app:locality}

\subsection{TFIM}

From~(\ref{eq:tfim-commutator}),
$C=[A,B]=2iJh\sum_j(Y_jZ_{j+1}+Z_jY_{j+1})$.
Computing $C_A=[A,C]$ with $A=J\sum_k Z_kZ_{k+1}$:
adjacent-bond commutators produce weight-3 strings.
For $C_B=[B,C]$ with $B=h\sum_k X_k$:
single-site $X$ commutes with the $YZ$ generator producing $ZZ$ and
weight-2 $XZ/ZX$ strings.

All generators in $\{A,B,C,C_A,C_B\}$ have Pauli weight $\leq 3$,
localized to two adjacent bonds.

\subsection{XXZ}

Identical locality argument holds; higher-order generators
remain on one to two bonds with Pauli weight $\leq 3$.

\section{Benchmark Protocol}
\label{app:protocol}

All benchmarks on $2^4=16$-dimensional Hilbert space.
Matrix exponentials computed via eigendecomposition
(\texttt{scipy.linalg.expm}).
Spectral-norm error: $\|U_{\rm exact}-U_{\rm approx}\|_2$.

\begin{table}[h]
  \caption{Benchmark parameter grids.}
  \label{tab:protocol}
  \begin{ruledtabular}
    \scriptsize
    \begin{tabular}{p{0.12\columnwidth}p{0.5\columnwidth}p{0.22\columnwidth}}
      Run & Parameters & Steps $N$ \\
      \colrule
      Local error & $n{=}4$, $J{=}1$, $h{=}0.5$,
                    $\delta t\in[0.02,1.0]$ & $N=1$ \\
      Global, $T{=}0.5$ & $n{=}4$, $J{=}1$, $h{=}0.5$ &
        $N\in\{2,4,\ldots,32\}$ \\
      Heatmap & $n{=}4$, $J{=}1$, $h{=}0.5$,
                $T\in\{0.25,\ldots,2\}$ & $N\in\{4,\ldots,24\}$ \\
    \end{tabular}
  \end{ruledtabular}
\end{table}

All results reproducible from \texttt{gen\_figure2.py} in the
companion repository.

\section{Lie GPT-2 Corrected Coefficient Derivation}
\label{app:gpt2-coeffs}

From Proposition~\ref{prop:bch}, the GPT-1 residual at $O(\delta t^3)$
is $-\frac{i\delta t^3}{6}C_A - \frac{i\delta t^3}{3}C_B$.
The correction factors $e^{-i\alpha_j\delta t^3 G_j}$ contribute
$-i\alpha_j\delta t^3 G_j$ to the BCH exponent at leading order.
Setting:
\begin{align}
  -i\alpha_2\delta t^3 C_A &= +\frac{i\delta t^3}{6}C_A
  \;\Rightarrow\; \alpha_2 = -\tfrac{1}{6}, \\
  -i\alpha_3\delta t^3 C_B &= +\frac{i\delta t^3}{3}C_B
  \;\Rightarrow\; \alpha_3 = -\tfrac{1}{3}.
\end{align}
With these coefficients:
$\log V_2(\delta t) = -i\delta t(A+B) + O(\delta t^4)$,
so $\|e^{-i\delta t H}-V_2\|_2 = O(\delta t^4)$.
The global error bound is
$O(t^4/N^3)$, matching Suzuki-4 asymptotic scaling.

\textbf{Why the sign matters.} Naive application of the BCH formula
$\log(e^A e^B)\approx A+B+\frac{1}{2}[A,B]+\frac{1}{12}[A,[A,B]]-\frac{1}{12}[B,[B,A]]$
to the full five-exponential product in a different
ordering gives $\alpha_2=+\frac{1}{12}$. This is incorrect because
the operator product in~(\ref{eq:gpt2-ansatz}) applies $C_A$ and
$C_B$ \emph{after} the GPT-1 base correction, so the relevant
residual is computed from $\log V_1$, not from $\log(e^{-idt B}e^{-idt A})$
directly.

\end{document}
